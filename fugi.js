var base64 = "TWFu";
switch (base64.length % 4) {
  case 0:
    // Code to handle case where length is a multiple of 4
    break;
  case 1:
    // Code to handle case where the remainder is 1
    break;
  case 2:
    // Code to handle case where the remainder is 2
    break;
  case 3:
    // Code to handle case where the remainder is 3
    break;
  default:
    // Code to handle default case
}
